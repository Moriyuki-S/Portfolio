---
import { cn } from '$lib/utils';
import SectionContainer from 'src/components/layout/sectionContainer.astro';
import { ProjectCardList } from 'src/features/projects/components/ProjectCardList';
import { projects } from 'src/features/projects/data/projects';
---

<SectionContainer sectionID="project">
    <hgroup class={cn(['mb-12', 'md:mb-16', 'text-center'])}>
        <p
            class={cn([
                'text-sm',
                'font-semibold',
                'uppercase',
                'tracking-[0.35em]',
                'text-slate-500',
                'dark:text-slate-400',
            ])}
        >
            Selected Works
        </p>
        <h2
            class={cn(
                ['mt-4', 'text-4xl', 'font-bold', 'text-gradient'],
                ['md:text-6xl'],
            )}
        >
            Projects
        </h2>
        <p
            class={cn([
                'mt-6',
                'text-base',
                'text-slate-600',
                'dark:text-slate-300',
                'leading-relaxed',
            ])}
        >
        これまでに携わったプロジェクトの一部を紹介します。各プロジェクトの詳細については、カードをクリックしてご覧ください。
        </p>
    </hgroup>
    <div
        class={cn(
            ['w-full']
        )}
    >
            <ProjectCardList projects={projects} client:visible />
    </div>
</SectionContainer>
